build:
  docker:
    web: server/Dockerfile

# If you have a specific run command, you can define it here.
# If you don't include this section, Heroku will use the CMD from your Dockerfile.
run:
  web: ./server/build/server
